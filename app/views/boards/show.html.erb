<h1><%= @board.name %></h1>
<%= link_to 'Back', boards_path, class: 'ui button' %>
<%= link_to 'New List', new_board_list_path(@board), class: 'ui button' %>
<% @board.lists.each do |list| %>
  <%= list.name %>
  <% list.tasks.each do |task| %>
    <%= task.body %>
  <% end %>
<% end %>
